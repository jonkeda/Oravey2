<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Oravey2.MapGen.App.ViewModels"
             x:Class="Oravey2.MapGen.App.Views.SettingsView"
             x:DataType="vm:SettingsViewModel"
             Title="Settings">

    <ScrollView Padding="16">
        <VerticalStackLayout Spacing="10">

            <Label Text="Model" FontAttributes="Bold" />
            <Picker ItemsSource="{Binding AvailableModels}" SelectedItem="{Binding SelectedModel}" />

            <BoxView HeightRequest="1" Color="Gray" Margin="0,10" />

            <Label Text="Bring Your Own Key (BYOK)" FontAttributes="Bold" />
            <HorizontalStackLayout Spacing="10">
                <Label Text="Enable BYOK" VerticalOptions="Center" />
                <Switch IsToggled="{Binding UseBYOK}" />
            </HorizontalStackLayout>

            <Label Text="Provider" />
            <Picker SelectedItem="{Binding ProviderType}" IsEnabled="{Binding UseBYOK}">
                <Picker.ItemsSource>
                    <x:Array Type="{x:Type x:String}">
                        <x:String>openai</x:String>
                        <x:String>azure</x:String>
                        <x:String>anthropic</x:String>
                    </x:Array>
                </Picker.ItemsSource>
            </Picker>

            <Label Text="Base URL" />
            <Entry Text="{Binding BaseUrl}" Placeholder="https://api.openai.com/v1" IsEnabled="{Binding UseBYOK}" />

            <Label Text="API Key" />
            <Entry Text="{Binding ApiKey}" IsPassword="True" Placeholder="sk-..." IsEnabled="{Binding UseBYOK}" />

            <BoxView HeightRequest="1" Color="Gray" Margin="0,10" />

            <Label Text="Export Path" FontAttributes="Bold" />
            <HorizontalStackLayout Spacing="10">
                <Entry Text="{Binding ExportPath}" Placeholder="Blueprints export folder" WidthRequest="400" />
                <Button Text="Browse..." Command="{Binding BrowseExportPathCommand}" />
            </HorizontalStackLayout>

            <Label Text="Game Install Path" FontAttributes="Bold" Margin="0,10,0,0" />
            <HorizontalStackLayout Spacing="10">
                <Entry Text="{Binding GameInstallPath}" Placeholder="Path to Oravey2.Windows output dir (e.g. bin/Debug/.../)" WidthRequest="400" />
                <Button Text="Browse..." Command="{Binding BrowseGameInstallPathCommand}" />
            </HorizontalStackLayout>

            <Label Text="Content Pack Catalog" FontAttributes="Bold" Margin="0,10,0,0" />
            <HorizontalStackLayout Spacing="10">
                <Entry Text="{Binding ContentPackCatalogPath}" Placeholder="catalog.json from a content pack (optional)" WidthRequest="400" />
                <Button Text="Browse..." Command="{Binding BrowseContentPackCommand}" />
            </HorizontalStackLayout>

            <BoxView HeightRequest="1" Color="Gray" Margin="0,10" />

            <Label Text="Copilot CLI Path (optional)" FontAttributes="Bold" />
            <Entry Text="{Binding CliPath}" Placeholder="Auto-detect" />

            <HorizontalStackLayout Spacing="10" Margin="0,20,0,0">
                <Button Text="Save Settings" Command="{Binding SaveSettingsCommand}" />
                <Button Text="Test Connection" Command="{Binding TestConnectionCommand}" />
                <Button Text="Login to GitHub" Command="{Binding LoginCopilotCommand}" />
            </HorizontalStackLayout>

            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />

            <Label Text="{Binding StatusMessage}" FontAttributes="Italic" TextColor="Gray" Margin="0,10,0,0" />

            <Button Text="Install GitHub Copilot (winget)" 
                    Command="{Binding InstallCopilotCommand}" 
                    IsVisible="{Binding ShowInstallCopilot}"
                    BackgroundColor="#238636"
                    TextColor="White"
                    Margin="0,5,0,0" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
