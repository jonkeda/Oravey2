<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Oravey2.MapGen.App.Views.TownSelectionStepView">
    <VerticalStackLayout Spacing="16" Padding="8">
        <Label Text="④ Town Selection" FontSize="24" FontAttributes="Bold" />
        <Label Text="Use LLM to select or discover towns for the region. Supports Mode A (discover) and Mode B (select from list)."
               TextColor="#AAAAAA" />
        <Label Text="Not yet implemented — will be completed in step 06." TextColor="#666666" FontSize="12" />
        <Button Text="Save &amp; Next →" IsEnabled="False" HorizontalOptions="Start" />
    </VerticalStackLayout>
</ContentView>
