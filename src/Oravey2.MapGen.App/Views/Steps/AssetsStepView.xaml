<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Oravey2.MapGen.App.Views.AssetsStepView">
    <VerticalStackLayout Spacing="16" Padding="8">
        <Label Text="⑦ 3D Assets" FontSize="24" FontAttributes="Bold" />
        <Label Text="Generate unique 3D assets via Meshy API. Create one at a time, review, accept or reject."
               TextColor="#AAAAAA" />
        <Label Text="Not yet implemented — will be completed in step 09." TextColor="#666666" FontSize="12" />
        <Button Text="Next →" IsEnabled="False" HorizontalOptions="Start" />
    </VerticalStackLayout>
</ContentView>
